spring:
  application:
    name: pres-server

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

jwt:
  issuer: pres-server
  secretKey: pres-secret-key

  datasource:
    url: jdbc:mysql://localhost:3306/pres_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useUnicode=true
    username: root
    password: rkskekfk1
    driver-class-name: com.mysql.cj.jdbc.Driver
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: 98b9a68288985b80e9afd47551cbe3da
            client-secret: JBDKAr2PzXcmKCAPhv4vuRTqMDhPjrkT
            redirect-uri: http://localhost:8080/callback
            authorization-grant-type: authorization_code
            client-authentication-method: POST
            client-name: Kakao
            scope:
              - profile_nickname
              - account_email
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

  redis:
    host: localhost
    port: 6379

  mail:
    host: smtp.gmail.com
    port: 587
    username: jhsoo3811@gmail.com
    password: "fcxi ypbq wecc jfvz"
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com

server:
  port: 8080
  servlet:
    context-path: /api

springdoc:
  api-docs:
    path: /v3/api-docs
    group-enabled: true
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    display-operation-id: true
    default-model-rendering: example
    doc-expansion: none
    display-request-duration: true
  path-to-match: /api/**
  packages-to-scan: com.example.presserver.controller



management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /actuator
  endpoint:
    health:
      show-details: always
      redis:
        enabled: true
      db:
        enabled: true
      mail:
        enabled: true
      diskspace:
        enabled: true
  metrics:
    export:
      prometheus:
        enabled: true
        rsocket:
          enabled: true
          host: localhost
          port: 9090
          mode: SERVER
          mapping: pres-metric
kakao:
  client-id: 98b9a68288985b80e9afd47551cbe3da
  client-secret: JBDKAr2PzXcmKCAPhv4vuRTqMDhPjrkT
  kauth-host: https://kauth.kakao.com
  kapi-host: https://kapi.kakao.com
  redirect-uri: http://localhost:8080/callback